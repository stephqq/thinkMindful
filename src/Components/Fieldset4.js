import React from 'react';

const Fieldset4 = (props) => {
    return (
        <fieldset className="fs4">
            <legend></legend>

            <button type="submit">save thought record</button>
        </fieldset>
    )
}

export default Fieldset4;